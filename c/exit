#include <stdlib.h>
#include <stdio.h>
#include "swis.h"
#include "swis_os.h"

void I_Error(char *s)
{
#ifndef __riscos64
    printf("Failed: %s\n", s);
#else
    os_write0("Failed: ");
    os_write0(s);
    os_newline();
#endif
    exit(1);
}
